This is a simple Django-based interface for vms_manage.

To enable it in dashboard, install the vms_manage package and turn it 
on in steer/setting.py
- add 'vms_manage' to INSTALLED_APPS tuple;
and
in local/local_settings.py:
- add 'VmsManage' to 'dashboards' key in STEER_CONFIG.

